# Ignore everything by default (repo root)
*

# But keep .gitignore itself so rules persist
!.gitignore

# Keep the out folder and all its contents
!/out/
!/out/**

# (optional) keep a README at root
!README.md

# (optional) keep GitHub workflows
!/.github/
!/.github/**
